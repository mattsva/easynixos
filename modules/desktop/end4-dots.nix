# modules/desktop/end4-dots.nix
# System-level placeholder for the end4-dots choice. The shell is configured at the
# Home Manager layer, and this module keeps the selected shell in the same pattern as
# the other modules without forcing a non-existent package into the system closure.
{ ... }:
{
}
